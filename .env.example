# Server
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://pasindu_fullstack:YTelEQYjoKxsudFZ@cluster0.8cb65as.mongodb.net/?appName=Cluster0

# JWT — use a long random string in production
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# Email (Nodemailer) — optional for local dev; required for verification & notifications
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM="Handbag Boutique <noreply@example.com>"

# First admin email — first registered user with this email becomes admin (or use seed script)
ADMIN_EMAIL=admin@example.com

# Stripe (test keys: https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# OpenAI — optional; chatbot falls back to rule-based answers if unset
OPENAI_API_KEY=
